#include "BrickBreaker.h"

void BrickBreaker::init()
{
	//ColorRGBA playerColor = ColorRGBA(0, 0, 0, 255);
	//player->createCircle("player", 400, 550, 50, playerColor, 0, 1);
	//player->createQuadrilateral("player", 400, 550, playerColor, 100, 20, 0);
}

void BrickBreaker::update()
{
	//
}
